#include <bits/stdc++.h>
using namespace std;
#define ll long long

void solve() {
    int x,y,a,b; cin >> x >> y >> a >> b;
    cout << "1 1 "<< x <<" "<< y << endl;
           
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    ll t; 
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}